services:
  - type: web
    name: speech-to-text-backend
    runtime: node
    plan: free
    buildCommand: npm install
    startCommand: npm start
    env: node
    envVars:
      - key: NODE_ENV
        value: production
      - key: PORT
        sync: false
    healthCheckPath: /api/health
    autoDeploy: true
    branch: main
